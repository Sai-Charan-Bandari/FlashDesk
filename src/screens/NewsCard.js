import { View, Text } from 'react-native'
import React from 'react'
import { Center } from 'native-base'

const NewsCard = ({source,author,title,description}) => {
  return (
    <Center>
      <Text>NewsCard</Text>
      {/* {source}
      {author}
      {title}
      {description} */}
      hello
      hi kya 
      hua
      bhao
    </Center>
  )
}

export default NewsCard